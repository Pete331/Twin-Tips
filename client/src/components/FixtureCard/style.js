import { makeStyles } from '../../utils/muiStyles';

const useStyles = makeStyles(() => ({
    fill: {
        height: '100%',
        display: 'flex',
        flexFlow: 'column',
        flexGrow: 1
    }
}));

export default useStyles;