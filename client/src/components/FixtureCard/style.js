import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    fill: {
        height: '100%',
        display: 'flex',
        flexFlow: 'column',
        flexGrow: 1
    }
}));

export default useStyles;